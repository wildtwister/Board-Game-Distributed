import { TictactoecellDirective } from './tictactoecell.directive';

describe('TictactoecellDirective', () => {
  it('should create an instance', () => {
    const directive = new TictactoecellDirective();
    expect(directive).toBeTruthy();
  });
});
