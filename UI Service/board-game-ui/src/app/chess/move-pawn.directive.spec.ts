import { MovePawnDirective } from './move-pawn.directive';

describe('MovePawnDirective', () => {
  it('should create an instance', () => {
    const directive = new MovePawnDirective();
    expect(directive).toBeTruthy();
  });
});
